# 研究思路与发现

本文档记录整个研究的完整逻辑线：**我们做了什么、为什么这样做、每一步发现了什么、发现之后怎么想的**。

---

## 一、起点：任务与数据

**任务**：基于 SEED 数据集的脑电（EEG）三分类情绪识别（负面 / 中性 / 正面）。

**特征**：差分熵（Differential Entropy，DE），结构为 **(N, 5, 62)**——5 个频段（delta/theta/alpha/beta/gamma）× 62 个电极通道，展平后得到 310 维特征向量。

**评估协议**：两种划分策略，分别代表两个难度递增的应用场景：

| 策略 | 训练集 | 测试集 | 代表场景 |
|------|--------|--------|---------|
| Subject-Dependent | 同一人同一 session 的前 9 个试次 | 同一人后 6 个试次 | 已知用户的个性化模型 |
| LOSO（跨被试） | 另外 11 个人的全量数据 | 1 个陌生人的全量数据 | 面向未知用户的通用模型 |

研究问题从 Subject-Dependent 出发，逐步向更难、更有实际价值的 LOSO 推进。

---

## 二、第一阶段：被试依赖（Subject-Dependent）基线实验

### 实验设置

三个模型在 Subject-Dependent 下的结果：

| 模型 | 准确率 |
|------|:------:|
| SVM（RBF核） | **71.40%** ± 16.63% |
| MLP（310→256→128→3） | 68.07% ± 18.69% |
| MLP + 注意力（SE-Net风格） | 65.32% ± 16.70% |

### 发现与分析

**发现一：SVM 赢了 MLP，且 MLP + 注意力是最差的。**

直觉上注意力机制应该更好，结果却适得其反。原因在于：

- 每个 fold 的训练数据只有**同一个人同一个 session 约 1600 个样本**。
- SVM 是核方法，用支持向量建立决策边界，天然适合小样本，泛化能力强。
- MLP 有约 114K 参数，加了注意力后约 116K，参数更多，数据不够训，反而更容易过拟合。
- 注意力模块（BandAttention + ChannelAttention）的 Sigmoid 门控权重需要充足的数据才能学到有意义的频段/通道重要性，数据太少时它学到的是噪声。

> **结论**：被试依赖场景下数据量不足是核心瓶颈，不是模型表达能力不够，所以加更多参数反而有害。

**发现二：高标准差（±16-18%）说明个体差异巨大。**

不同被试的准确率差异可达 30-40%，说明脑电信号存在显著的个体特异性。这个观察为后续 LOSO 实验埋下了伏笔。

---

## 三、第二阶段：解决被试依赖的数据瓶颈——跨 session 迁移学习

### 动机

基线实验揭示问题根源是**同一个 session 内的数据太少**。SEED 数据集每个被试有 3 个 session，天然提供了同一个人在不同时间的额外数据，这是解决数据稀缺的最直接来源。

### 两个方案的对比设计

对于每个 fold（被试 s，session t）：

- **SVM + 跨 session**：把其他两个 session 的全量数据直接合并进训练集，单次 `fit()`
- **MLP + 跨 session 预训练**：第一阶段在其他两个 session 上预训练（lr=1e-3），第二阶段在目标 session 上以低学习率微调（lr=2e-4）

两个方案共享同一个核心逻辑：利用**同一个人不同时间的脑电**。区别在于 SVM 只能"合并数据一锅炖"，而 MLP 可以"先学通用模式，再适配当前场景"。

### 结果

| 模型 | 准确率 | vs 各自基线 |
|------|:------:|:-----------:|
| SVM（baseline） | 71.40% ± 16.63% | — |
| MLP（baseline） | 68.07% ± 18.69% | — |
| **SVM + 跨session** | **78.17%** ± 14.55% | **+6.77%** |
| **MLP + 跨session预训练** | **77.63%** ± 14.22% | **+9.56%** |

### 发现与分析

**发现三：SVM + 跨 session ≈ MLP + 跨 session（78.17% vs 77.63%），差距仅 0.54%。**

这个结果非常有信息量：它说明提升的主要来源是**同一个人的额外数据本身**（数据效应），而不是两阶段预训练/微调范式（方法效应）。换言之，对同一个人而言，多给数据比换方法更重要。

**发现四：两个方案的标准差都降低了（从 ±16-18% 降至 ±14%）。**

数据增多让模型对不同被试的表现更稳定，方差下降意味着鲁棒性提升。

**发现五：但无论如何，被试依赖仍然是个"奢侈"的场景。**

它要求对每个新用户都有已标注的历史脑电数据，实际应用中往往难以满足。这自然引出了更重要的问题：**如果用户是陌生人，怎么办？**

---

## 三·补充：进一步挖掘被试依赖——Session 间的分布漂移

### 动机

发现三表明 SVM+CS ≈ MLP+CS，提升主要来自额外数据而非预训练/微调范式本身。但这个结论背后有一个隐藏问题：**跨 session 的数据直接合并，默认不同 session 的特征分布一致。**

事实上，EEG 的 DE 特征在不同日期的录制之间存在系统性漂移——电极阻抗、被试疲劳状态、皮肤导电性等都会变化。这种**session 内的协变量偏移（covariate shift）**与跨被试的域偏移本质相同，只是量级更小。

如果预训练时显式对齐 source sessions 与 target session 的特征分布，能否在现有 ~78% 的基础上进一步提升？

### 两种方法

**MLP + CS + CORAL（预训练阶段加分布对齐）**：在 Phase 1 预训练时，同时对 source session 特征和 target session 无标签测试特征施加 CORAL loss，使预训练特征对 session 间偏移具有鲁棒性，再进入 Phase 2 微调。

**MLP + CS + AdaBN（统计量替换，零标签自适应）**：预训练完成后，不做任何梯度更新，仅将 BN 层的 running mean/var 从 source session 的统计量替换为 target session 的统计量——这一步完全无需标签，本质是"告诉模型现在的数据长什么样"。随后可选地再接一轮微调。

### 结果

| 方法 | 准确率 | vs MLP+CS |
|------|:------:|:---------:|
| MLP + Cross-Session（基准） | 77.63% ± 14.22% | — |
| MLP + CS + AdaBN（无微调） | 80.28% ± 14.31% | +2.65% |
| MLP + CS + AdaBN + Finetune | 80.46% ± 13.73% | +2.83% |
| **MLP + CS + CORAL** | **82.09% ± 10.78%** | **+4.46%** |

### 发现与分析

**发现 A：Session 间确实存在可利用的分布漂移。** 三种方法均优于不做对齐的 MLP+CS（77.63%），说明跨 session 的协变量偏移是真实的，并非噪声。

**发现 B：AdaBN 的"零标签免费午餐"是真实的（+2.65%）。** 仅替换 BN 统计量、不做任何梯度更新，就能获得 +2.65% 的提升。这证明 BN 的 running mean/var 在跨 session 迁移中是重要的信息瓶颈——它们编码的是 source session 的分布，而非 target session 的。

**发现 C：AdaBN 微调带来的额外增益微乎其微（+0.18%）。** AdaBN+FT（80.46%）与 AdaBN-only（80.28%）几乎持平，说明 BN 统计量替换已经完成了分布对齐的大部分工作，后续微调对 session 偏移的贡献有限——微调更多是在利用目标 session 的标注数据而非做分布自适应。

**发现 D：CORAL 的显式协方差对齐效果最好（82.09% ± 10.78%）。** CORAL 不只是替换一阶统计量（均值/方差），而是在训练时持续对齐二阶统计量（协方差矩阵），使特征空间的整体"形状"一致。更重要的是，**CORAL 将标准差从 ±14.22% 降至 ±10.78%**——这是所有方法中最低的，说明 CORAL 的对齐让不同 session 的被试受益更均匀，提升了方法的鲁棒性。

**发现 E：标准差随对齐强度单调下降。** MLP+CS（±14.22%）→ AdaBN+FT（±13.73%）→ CORAL（±10.78%），分布对齐越彻底，不同 session 间的性能波动越小。这与 LOSO 域适应中的规律完全一致（域适应后 LOSO 标准差也系统下降）。

---

## 四、过渡：从被试依赖到跨被试——提出真正的挑战

被试依赖的本质是"熟人测试"——模型见过这个人。但现实中的 BCI 系统面对的是**陌生用户**，这正是 LOSO 评估的意义所在。

跨被试面临的核心困难是**脑电信号的个体差异**：不同人在相同情绪状态下的脑电模式差异很大，就像不同人写同一个字的笔迹。模型在 11 个人身上学到的"情绪模式"，未必适用于第 12 个陌生人。这种差异在机器学习中被称为**域偏移（domain shift）**。

---

## 五、第三阶段：LOSO 基线实验——意外的反转

### 结果

| 模型 | LOSO 准确率 |
|------|:-----------:|
| SVM（RBF核） | 53.60% ± 12.20% |
| MLP（baseline） | **56.90%** ± 13.17% |
| MLP + 注意力 | 55.27% ± 14.55% |

### 发现与分析

**发现六：在 LOSO 场景下，MLP 反超了 SVM（56.90% vs 53.60%）。**

这与被试依赖的结论完全相反，原因是：在 LOSO 中训练集包含 11 个被试的全量数据，样本量远大于被试依赖场景，神经网络终于有了足够的数据，MLP 的表达能力得以发挥，而 SVM 的样本效率优势在大数据量下不再突出。

**发现七：注意力机制在 LOSO 下仍然无效（55.27% < 56.90%）。**

这是更深刻的发现。在跨被试场景中，不同人的"重要频段/通道"不完全一样。注意力权重在 11 个源被试上学到的"哪个通道重要"，不一定适用于目标被试。注意力模块增加了参数，却无法提供有效的跨人泛化信号，反而引入了噪声。

**发现八：LOSO 整体准确率只有 53-57%，仅略高于随机猜测（33%的2倍）。**

三分类任务的随机基线为 33.3%。53-57% 意味着模型只是"比随机好一点"，远未达到实用水平。这说明仅靠模型结构的改进是不够的，**必须正面解决个体差异（域偏移）这个根本问题**。

---

## 六、第四阶段：域适应——正面解决跨人泛化问题

### 动机

LOSO 的低准确率本质是域偏移问题：训练域（11个被试）和测试域（1个陌生被试）的特征分布存在系统性偏差。注意力机制处理的是"特征重要性"，而不是"分布对齐"，因此无法解决这个问题。

域适应的核心思想是：在训练时利用目标被试的**无标签数据**（测试集的特征，不用标签）来引导模型学习对个体差异不敏感的通用表征。

### 三种方法的设计逻辑

**CORAL（相关对齐）**：对齐源域和目标域特征的**协方差矩阵**（二阶统计量）。本质是让两个域的特征"形状"一致。实现最简单，只需在 MLP 的倒数第二层特征上加一个 loss 项。

**MMD（最大均值差异）**：用多个不同尺度的 RBF 核函数衡量两个域特征分布的差距，覆盖多个尺度上的分布差异。比 CORAL 更全面，计算量略高。

**DANN（域对抗神经网络）**：在特征提取器后加一个"域判别器"，通过梯度反转层（GRL）让特征提取器主动"欺骗"判别器——如果判别器无法区分源域和目标域，说明特征已经对个体差异免疫。这是三者中最复杂的对抗训练范式，α 从 0 逐渐增大到 1，保证训练初期不破坏分类器。

### 结果

| 方法 | LOSO 准确率 | vs MLP 基线 |
|------|:-----------:|:-----------:|
| MLP（baseline） | 56.90% ± 13.17% | — |
| MLP + 注意力 | 55.27% ± 14.55% | −1.63% |
| **MLP + CORAL** | **66.06%** ± 12.65% | **+9.16%** |
| **MLP + DANN** | **66.79%** ± 12.23% | **+9.89%** |
| **MLP + MMD** | **67.45%** ± 11.83% | **+10.55%** |

### 发现与分析

**发现九：三种域适应方法都带来了约 +9-11% 的大幅提升，且标准差全部下降。**

这是本研究最核心的正面发现。三种完全不同的域适应机制（统计对齐、核方法、对抗训练）都取得了近似的大幅提升，说明**提升的根本原因是正确解决了域偏移问题**，而不是某种方法特有的技巧。

**发现十：MMD（67.45%）略优于 DANN（66.79%）和 CORAL（66.06%）。**

MMD 使用多个尺度的 RBF 核（σ = 0.5, 1, 2, 5, 10），能同时捕捉不同尺度上的分布差异。EEG 的 DE 特征在不同被试间的偏差可能发生在不同的特征维度尺度上，多核覆盖使 MMD 具有更强的适应性。DANN 虽然理论上最强，但对抗训练的稳定性依赖超参数调整，在有限 epoch 下可能未充分收敛。CORAL 最简单，效果也略低，但与 MMD 差距仅 1.4%，性价比最高。

**发现十一：标准差从 ±13.17% 降至 ±11-12%，说明域适应提升了模型的个体鲁棒性。**

准确率的提升和方差的下降同时发生，说明域适应不只是提升平均表现，而是让模型对各种"难"被试也更稳定。

---

## 七、整体研究框架与核心主张

### 研究框架

本研究在两个层次上系统研究了**迁移学习与域适应如何提升脑电情绪识别**：

```
被试依赖（Subject-Dependent）
  ├── 基线：SVM > MLP > MLP+Attention
  │          (数据稀缺时，小模型更优)
  ├── 跨 session 迁移学习
  │            SVM+CS ≈ MLP+CS  (+6~10%)
  │            → 同人数据是最有效的补充，数据效应 > 方法效应
  └── Session 间域适应（CORAL / AdaBN）
               CORAL: 82.09% ± 10.78%  (+4.46% vs MLP+CS)
               AdaBN: 80.46% ± 13.73%  (+2.83%，其中2.65%零标签免费)
               → Session间存在可对齐的分布漂移；CORAL协方差对齐效果最优且方差最小

LOSO（跨被试）
  ├── 基线：MLP > SVM > MLP+Attention
  │          (数据充足时，神经网络反超SVM；注意力无法解决域偏移)
  └── 域适应（CORAL / MMD / DANN）
             均提升约 +9-11%，标准差下降
             → 域偏移是LOSO低准确率的根本原因，对齐分布是正确方向
```

### 核心主张

> **在脑电情绪识别中，分布偏移——无论是 session 间的协变量漂移还是被试间的域偏移——是限制泛化性能的根本瓶颈，而非模型容量或特征重要性。在被试依赖场景中，跨 session 域适应（CORAL 协方差对齐、AdaBN 统计量替换）可将准确率从 ~78% 进一步推至 82%，且显著降低方差；在跨被试场景中，分布对齐（CORAL / MMD / DANN）带来约 +10% 的系统性提升。分布对齐在两个层次上均有效，且对齐越彻底，个体间的性能波动越小。**

---

## 八、局限性与未来方向

1. **LOSO 准确率仍有提升空间**：域适应后最高 67.45%，对于三分类任务仍有较大提升余地。更复杂的图神经网络（GCN/GAT 对电极拓扑建模）或自监督预训练可能进一步提升。

2. **跨 session 域适应已部分解决数据效应瓶颈**：加入 CORAL 对齐后，MLP+CS+CORAL（82.09%）明显优于 SVM+CS（78.17%），说明两阶段预训练 + 分布对齐的组合确实超越了纯数据堆叠。AdaBN 的零标签增益（+2.65%）也表明 BN 统计量是一个低成本但有效的适应手段。未来可探索更强的对齐方法（如 DANN 在跨 session 场景的应用）。

3. **λ 超参数未系统调优**：CORAL/MMD/DANN 的 λ 均使用默认值 1.0，系统调参可能带来额外提升。
